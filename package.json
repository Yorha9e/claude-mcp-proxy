{
  "name": "mcp-proxy",
  "version": "0.1.0",
  "type": "module",
  "description": "MCP Proxy with per-tool filtering and hot-reload",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "start": "node scripts/proxy-server.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "chokidar": "^4.0.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
